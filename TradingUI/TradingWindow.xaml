<Window x:Class="TradingUI.TradingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TradingUI" xmlns:viewmodels="clr-namespace:TradingUI.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:TradingViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="DarkGray">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="Trading Application" FontSize="16" FontWeight="Bold" Margin="10"/>
        <DataGrid Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Accounts}" AutoGenerateColumns="False" Margin="10" SelectionMode="Single" SelectedItem="{Binding SelectedAccount}" VerticalScrollBarVisibility="Auto" FontSize="10" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" HorizontalScrollBarVisibility="Visible">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ClientName" Binding="{Binding ClientName}" Width="*" />
                <DataGridTextColumn Header="ClientNumber" Binding="{Binding AccountNumber}" Width="*" />
                <DataGridTextColumn Header="CashBalance" Binding="{Binding CashBalance, StringFormat={}{0:F2}}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Account Number:" FontSize="12" FontWeight="Bold" Margin="5" HorizontalAlignment="Right"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Client Name:" FontSize="12" FontWeight="Bold" Margin="5" HorizontalAlignment="Right"/>
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Cash Balance:" FontSize="12" FontWeight="Bold" Margin="5" HorizontalAlignment="Right"/>
            <TextBlock Grid.Row="0" Grid.Column="1"  Text="{Binding SelectedAccount.AccountNumber}" FontSize="12" FontWeight="Bold" Margin="5" HorizontalAlignment="Left"/>
            <TextBlock Grid.Row="1" Grid.Column="1"  Text="{Binding SelectedAccount.ClientName}" FontSize="12" FontWeight="Bold" Margin="5" HorizontalAlignment="Left"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedAccount.CashBalance, StringFormat={}{0:F2}}" FontSize="12" FontWeight="Bold" Margin="5" HorizontalAlignment="Left">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="Transparent"/>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBalanceChanged}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation
                                    Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                    To="Red"
                                    Duration="0:0:0.5"
                                    AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <Button Grid.Row="3" Grid.Column="0" Content="Refresh" Command="{Binding RefreshCommand}" Margin="18" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Button Grid.Row="3" Grid.Column="1" Content="Place Order" Command="{Binding PlaceOrderCommand}" Margin="18" HorizontalAlignment="Right" VerticalAlignment="Top"/>
            <DataGrid Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding PositionDetails}" AutoGenerateColumns="False" Margin="10" SelectionMode="Single" VerticalScrollBarVisibility="Auto" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" HorizontalScrollBarVisibility="Visible">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*" />
                    <DataGridTextColumn Header="AverageCostPerShare" Binding="{Binding AverageCostPerShare, StringFormat={}{0:F2}}" Width="2*" />
                    <DataGridTextColumn Header="CurrentPrice" Binding="{Binding CurrentPrice, StringFormat={}{0:F2}}" Width="*" />
                    <DataGridTextColumn Header="CurrentValue" Binding="{Binding CurrentValue, StringFormat={}{0:F2}}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
            <TextBlock Grid.Row="5" Text="{Binding ConnectionStatus}" FontSize="10" Foreground="Red" Margin="5"/>
        </Grid>
    </Grid>
</Window>
